# Python 3.10+ 推荐，3.11、3.12、3.13、3.14 都兼容
# python>=3.10.0

# 核心依赖（使用>=确保能安装最新兼容版本）
pandas>=2.0.0
dask>=2023.0.0
openpyxl>=3.0.0
jieba>=0.42.0
snownlp>=0.12.0
matplotlib>=3.7.0
seaborn>=0.12.0
pyinstaller>=5.0.0
rank_bm25>=0.2.0

# 可选依赖（用于更好的性能）
# numpy>=1.24.0  # pandas会自动安装
# pillow>=10.0.0  # matplotlib会自动安装

